class Solution {
    public boolean hasDuplicate(int[] nums) {
        Set<Integer> mySet = new HashSet<>();
        for(int i: nums){
            mySet.add(i);
        }
        if(mySet.size() == nums.length){
            return false;
        }
        return true;
    }
}
