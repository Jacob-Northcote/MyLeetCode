class Solution {

    public String encode(List<String> strs) {
        S
        for(int i: strs){
            input.append(i.length).append('#').append(i);
        }
        return input.toString();
    }

    public List<String> decode(String str) {
        ArrayList<String> strings = new ArrayList<>();
        int i = 0;

        while(i < str.length()){

             while (str.charAt(j) != '#') {

             }

        }
        return strings;
    }
}
